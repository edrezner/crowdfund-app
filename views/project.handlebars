<div>
  <h2>{{name}}</h2>
  <p>{{description}}</p>
  <p>Created by {{user.name}}</p>
  <p>
    <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{formatAmount funding_goal}}</span>
    needed
  </p>
  <p><span for="img" aria-label="money">💲</span>
    <span>{{formatAmount funded_amount}}
      raised</span></p>

  <div
    style="background:#ccc; border-radius:5px; overflow:hidden; width:100%; height:20px;"
  >
    <div style="background:#4caf50; height:100%; width:{{progressPercent}}%;">
    </div>
  </div>
  <span>{{formatPercent progressPercent}}</span>
  <p>
    <button id="donate-btn" class="btn btn-primary">Fund this Project!</button>
  </p>
</div>

<script src="/js/project.js"></script>